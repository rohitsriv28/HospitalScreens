package com.example.hospital.ui.components

import androidx.compose.foundation.layout.size
import androidx.compose.ui.Modifier
import androidx.compose.ui.graphics.Color
import androidx.compose.ui.unit.dp

object MyDefaultValues {
    val baseColor = Color(2, 196, 177, 155)
    val defaultSpace = Modifier.size(0.dp, 24.dp)
    val appBarLogo = Color.Black
    val logoSize = Modifier.size(70.dp, 70.dp)
//    val textSize =
}